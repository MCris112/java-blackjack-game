public class Main {

    static void main() {

        // Creamos la clase para poder llamar la function init que
        //iniciara el juego con el menu en carga
        Game game = new Game();

        game.showMenu();
    }
}
